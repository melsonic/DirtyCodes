#include<stdio.h>

int main(int argc, char** argv){
  // define a, b, c
  int a = 5, b = 3, c = 10;
  // add and store all of them into a new variable d
  int d = a + b + c;
  /* print the value of d */
  printf("d : %d\n", d);
  return 0;
}